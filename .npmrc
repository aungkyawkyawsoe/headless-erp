# @mmbix packages resolve through GitHub Packages (publish only — installs use
# workspace: links). Auth is developer/CI-specific: set the token in your
# USER-level .npmrc instead of committing it here:
#   pnpm config set //npm.pkg.github.com/:_authToken <token> --global
@mmbix:registry=https://npm.pkg.github.com/
