/**
 * {{id}} Module — Frontend Plugin
 *
 * Auto-generated by `headless module create {{id}} --frontend`
 *
 * This file is SELF-CONTAINED by design: the frontend shell is DB-driven
 * (the sidebar dock reads GET /api/modules), so there is no plugin-registry
 * barrel to import into. It just exports a React component plus a small meta
 * object — wire it into your app wherever you need it (or leave it as a
 * reference implementation and drive everything from the backend).
 *
 * Module: {{id}}
 */
import type { ReactNode } from 'react';

/** Minimal, dependency-free metadata for this module. */
export const {{id}}Meta = {
	id: '{{id}}',
	name: '{{id}}',
	description: '{{id}} module — generated by headless module create',
} as const;

/**
 * A sample page component. `headless module create` registers the module in
 * the backend (GET /api/modules), so no registration call is needed here.
 */
export default function {{id}}Page(): ReactNode {
	return (
		<div style=\{{ padding: '1.5rem' }}>
			<h2 style=\{{ marginTop: 0 }}>Module: {{id}}</h2>
			<p>This page was scaffolded by the headless CLI. Edit it to build out the module.</p>
		</div>
	);
}
