/**
 * Hook: {{description}}
 * Collection: {{collection}} | Event: {{event}} | Priority: {{priority}} | Timeout: {{timeoutMs}}ms
 * Auto-generated by `headless hook:add`
 */
ctx.hooks.on({
	collection: '{{collection}}',
	event: '{{event}}',
	priority: {{priority}},
	timeoutMs: {{timeoutMs}},
	handler: async (doc, rawD1, auth) => {
		const db = new D1Client(rawD1);
		{{serviceInit}}

		// TODO: Implement your business logic here
		// doc contains the entity data
		// Use db.all(), db.run(), db.batch() for database operations
		// Return { abort: true, error: '...' } to reject the operation
	},
});
