{
	"name": "@mmbix/events",
	"version": "0.1.0",
	"private": true,
	"description": "Event bus — Cloudflare Queues pub/sub with D1 subscribers, delivery dedupe, webhook + handler actions",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./package.json": "./package.json"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@mmbix/core": "workspace:*",
		"@mmbix/types": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"typescript": "catalog:"
	}
}
