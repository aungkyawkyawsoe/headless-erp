{
	"name": "@mmbix/utils",
	"version": "0.8.0",
	"private": true,
	"description": "Shared utilities — sanitizers, validators, error classes",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "vitest run"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
