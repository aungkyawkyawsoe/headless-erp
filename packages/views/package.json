{
	"name": "@mmbix/views",
	"version": "0.1.0",
	"private": true,
	"description": "Materialized views — declarative projections over tables, refreshed on demand or on a schedule (D1)",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./package.json": "./package.json"
	},
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@mmbix/core": "workspace:*",
		"@mmbix/types": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"typescript": "catalog:"
	}
}
